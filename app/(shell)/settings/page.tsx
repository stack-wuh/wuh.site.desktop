import { SettingsPage } from '../../../components/settings/SettingsPage'

/** 设置页 = 右栏普通页面（两栏布局起不再是全屏视图），返回语义走 router */
export default function Page(): React.JSX.Element {
  return <SettingsPage />
}
